# Expo
.expo/
.expo-shared/
*.log

# Node
node_modules/

# Env
.env
.env.*

# iOS
ios/Pods/
ios/build/

# Android
android/

# macOS
.DS_Store
# OpenClaw / agent state
.omc/

# Expo local state
.expo/
*.log

# Sessions / secrets
scripts/.giga-session*.json
scripts/.giga-chrome-profile/
logs/
tmp/
.env
.env.*

# Local planner report artifacts (regenerated; not source)
reports/giga-auto-publish/

# Local iOS release state (build id recorded for the submit stage; not source)
reports/release/

# Manual emergency product upload — generated plan/apply reports + operator-specific SKU inputs (not source)
reports/giga-manual-product/
scripts/manual-inputs/

# Local backups, debug output, and Deno lockfile (not part of the app/release)
backups/
scripts/debug-output/
deno.lock

# Dropship delivery-fee credentials (untracked); the .example placeholder IS tracked.
# (.env.* already ignores the real file; this is explicit and re-includes the example.)
.env.giga-delivery.local
!.env.giga-delivery.local.example
